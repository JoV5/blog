function shellSort(arr) {
    let N = arr.length;
    let i, j, Increment;
    let tmp;

    for (Increment = N / 2; Increment > 0; parseInt(Increment /= 2)) {
        for (i = Increment; i < N; i++) {
            
        }
    }
}